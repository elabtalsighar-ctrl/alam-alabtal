services:
  - type: web
    name: alam-alabtal
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /api/settings
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
